{% extends 'layout.twig' %}

{% block body %}
  <div class="product">
    <div class="row first_row_product">
      <img src="{{ data.logo }}">
      <h4>{{ data.url.href }}</h4>
    </div>
    {% if data.image != "error" %}
      <div class="row second_row_product">
        <img src="{{ data.image }}">
      </div>
      <div class="row">
        <a href="/"><button id="btn" type="button" class="neu-light btn-action" style="margin-right: 10px">Retourner au menu</button></a>
        <a href="{{ data.aliseeks }}" target="_blank"><button id="btn" type="button" class="neu-light btn-action" >Retrouver le vrai article au meilleur prix</button></a>
      </div>
    {% elseif data.site_already_save == false %}
      <div class="row second_row_product">
        <p>Probablement pas un site de dropshipping, si vous êtes certains que c'est du dropshipping, veuillez envoyer le lien du site dans la section <a href="/contact">contact</a>
           ou scanner un article de ce site.</p>
      </div>
      <div class="row">
        <a href="/"><button id="btn" type="button" class="neu-light btn-action" >Revenir au menu d'acceuil</button></a>
      </div>
    {% else %}
      <div class="row second_row_product">
        <p>Ce site est répértorié sur notre plateforme, vous pouvez voir les produits scannés <a href="{{data.site_already_save}}">ici.</a></p>
      </div>
      <div class="row">
        <a href="/"><button id="btn" type="button" class="neu-light btn-action" >Revenir au menu d'acceuil</button></a>
      </div>
    {% endif %}
  </div>
{% endblock %}
